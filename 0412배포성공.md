# 🎮 AGame MVP EC2 배포 메모

## ✅ 배포 개요
- **Frontend**: React (포트 3000 개발, 실제 배포는 Spring static에 포함)
- **Backend**: Spring Boot (8080 포트)
- **서버 환경**: AWS EC2 Ubuntu 24.04 LTS
- **배포 방식**: Docker + S3 + CodeDeploy 준비 중 (현재 수동 배포)

---

## 📦 프로젝트 구조
```bash
~/AGame_MVP/
├── MVP_Final/
│   ├── mvp-agame-front/   # React
│   └── MVP_AGame_SPRING/  # Spring Boot
├── deploy.sh
├── Dockerfile
├── appspec.yml
└── AGame_MVP.zip          # 압축파일 (S3 업로드용)
```

---

## 🐳 Docker + 배포 스크립트 (`deploy.sh`)

### deploy.sh 주요 단계
1. React 빌드 결과를 Spring `static/` 폴더로 복사
2. Maven 빌드 (JAR 생성)
3. Docker 컨테이너/이미지 정리
4. 새 Docker 이미지 빌드 및 컨테이너 실행
5. 컨테이너 로그 출력

### Dockerfile (Spring 기준)
```dockerfile
FROM openjdk:17-jdk
EXPOSE 8080
ARG JAR_FILE=target/MVP_AGame_SPRING-0.0.1-SNAPSHOT.jar
COPY ${JAR_FILE} app.jar
ENTRYPOINT ["java","-jar","/app.jar"]
```

---

## 📂 S3 압축/업로드/복구 흐름
1. 로컬에서 `AGame_MVP.zip` 생성 (루트에 `deploy.sh`, `Dockerfile`, `appspec.yml`, `MVP_Final/` 포함)
2. S3 업로드: `aws s3 cp AGame_MVP.zip s3://agame-bucket/deploy/`
3. EC2에서 다운로드 및 unzip
```bash
aws s3 cp s3://agame-bucket/deploy/AGame_MVP.zip .
unzip -o AGame_MVP.zip
```

---

## 💡 자주 발생한 문제 해결

### 🧹 디스크 공간 부족
```bash
docker system prune -a --volumes -f
sudo apt-get clean
sudo rm -rf /var/log/*
rm -rf ~/AGame_MVP/MVP_Final/mvp-agame-front/node_modules
```

### ⚠️ Maven Wrapper 오류
- `.mvn/wrapper/maven-wrapper.properties` 없음 → `./mvnw` 실행 불가
- 해결: `sudo apt install maven` 후 `mvn clean package -DskipTests`로 직접 빌드

### 📂 JAR 파일이 Docker에 복사되지 않는 문제
- `target/`에 JAR 없는 경우 → 빌드가 안 됐거나 경로 오류
- 해결: 위 빌드 후 다시 deploy.sh 실행

### ⚠️ React 라우팅 문제 (흰 화면)
- 해결: Spring에 다음 Controller 추가
```java
@Controller
public class RouteController {
    @RequestMapping("/{path:[^\\.]*}")
    public String forward() {
        return "forward:/";
    }
}
```

---

## 🔐 접속
- 접속 URL 예시: `http://13.209.50.203:8080`
- React + Spring 통합된 정적 파일은 8080에서 제공됨

---

## ✍️ 향후 TODO
- [ ] EC2에 MongoDB 설치 및 연결 확인 (`localhost:27017` 오류 있음)
- [ ] CodeDeploy + GitHub Actions 자동화
- [ ] `.mvn` 복구 or 빌드 경량화
- [ ] EC2 용량 확장 고려

---

배포 성공일: **2025-04-12** 🚀

> "기록은 최고의 디버깅 도구다." – 잘했다 정말! 🙌

